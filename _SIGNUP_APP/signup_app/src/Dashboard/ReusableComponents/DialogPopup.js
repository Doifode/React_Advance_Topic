// import React from "react";
//  import {Dialog ,Typography,DialogContent ,DialogActions,DialogTitle}from "@mui/material"
// // import { Close } from "@material-ui/icons";
// const DialogPopup = (props) => {
//   const { children, onClose, footer, title, dividers } = props;

//   return (
//     <>
//       <Dialog {...props}>
//         {onClose && (
//           <Typography onClick={onClose}>
//             {/* <Close className="pointer float-right m-1" /> */}
//           </Typography>
//         )}
//         {title && <DialogTitle>{title}</DialogTitle>}
//         <DialogContent style={{ width: "556px" }} {...(title && dividers)}>
//           {children}
//         </DialogContent>
//         {footer && <DialogActions>{footer}</DialogActions>}
//       </Dialog>
//     </>
//   );
// };

// export default DialogPopup;
